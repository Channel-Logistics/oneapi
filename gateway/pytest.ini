[pytest]
addopts = --strict-markers --cov=main --cov-branch --cov-report=term-missing
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    unit: fast tests with fakes/mocks
    integration: in-proc + external dependencies
    e2e: live service via HTTP
